import React from "react";
import {bookType} from "../types/types";

type PropsType = {
    book: bookType
}

const BookCard: React.FC<PropsType> = () => {

    return (
        <div>
book
        </div>
    )
}

export {BookCard}